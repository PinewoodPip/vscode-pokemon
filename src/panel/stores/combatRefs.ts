/** Mutable ref registry populated by React combat components. Used by CombatUIManager for overhead animations. */
export const combatRefs = {
    playerSectionEl: null as HTMLElement | null,
    enemySectionEl: null as HTMLElement | null,
};
